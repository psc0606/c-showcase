#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <math.h>

/**
 * 模拟大量计算，测试系统状态
 * usage: gcc -lm stat1.c
 */
void run_status(void) {
	double pi = M_PI;
	double pisqrt;
	long i;
	while(1){
		pisqrt = sqrt(pi);
	}
}

int main (void) {
	run_status();

	exit(EXIT_SUCCESS);
}
