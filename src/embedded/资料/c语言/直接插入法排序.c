/*--------------------------------
	直接插入法排序
----------------------------------
*/

#include <stdio.h>
void main()
{
	int i, j, k, a[10]={2,3,5,1,6,7,9,8,4,10};
	for (i = 1; i < 10; i++)
	{
		j = i -1;
		k = a[i];
		while (j >= 0 && k > a[j])
		{
			a[i--] = a[j--];
		}
		a[i] = k;
		
	
/*		for (j = i-1; j >= 0; j--)
		{
			if (a[j+1] > a[j])
			{
				k = a[j+1];
				a[j+1] = a[j];
				a[j] = k;
			}
		}
*/	}

		
	for (i = 0; i < 10; i++)
	{
		printf("%3d",a[i]);
	}
}
